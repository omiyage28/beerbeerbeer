<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LocalBeer</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
    <% if user_signed_in? %>
       <div class="greeting"> 
        <%= "cheeeeeeeers!!"%>
        <%= link_to current_user.name ,user_path(current_user.id), class: :greeting__link%>
       </div> 
    <% end %>
      <h1 class="logo"><a href="/">Local Beer</a></h1>
       <h3 class="description">地ビール(クラフトビール)とは,</h3>
          <h4>小規模な醸造所がつくる、多様で個性的なビールのこと</h4>

          </div>
          <% if user_signed_in? %>
            <div class="nav__right">
              <%= link_to "SIGN OUT", destroy_user_session_path, method: :delete ,class: :nav__logout %>
              <%= link_to "POST", new_image_path, class: :nav__btn %>
            </div>
            <% else %>
            <div class="nav__right">
              <%= link_to "SIGN IN", new_user_session_path, class: :nav__btn %>
              <%= link_to "SIGN UP", new_user_registration_path, class: :nav__btn_signup %>
            </div>
            <% end %>
  
          <div class="nav__right">
            <div class="lets_learn">
             <div class="ribbon11-wrapper">
             <h3 class="ribbon11">Let's Learn BEER!!</h3>
                 <%= link_to "MAP", images_map_path, method: :get ,class: :nav__logout %>
              </div>
            </div>
          </div>
      </div>

    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">Local Beer </p>
    </footer>
    
  </body>
</html>
